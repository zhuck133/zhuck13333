# Получаем список элементов от пользователя
elements = input("Введите числа через пробел: ").split()
power = int(input("Введите степень: "))

result = []
for element in elements:
    try:
        # Пробуем преобразовать в число
        num = float(element)
        # Возводим в степень (учитываем отрицательные числа)
        result.append(str(num ** power))
    except ValueError:
        # Если не число - умножаем строку
        result.append(element * power)

print("Вывод:", " ".join(result))
